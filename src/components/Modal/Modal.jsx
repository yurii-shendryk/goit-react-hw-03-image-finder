import React from 'react';
import PropTypes from 'prop-types';
import './Modal.scss';

const Modal = props => (
  <div className="Overlay">
    <div className="Modal">
      <img src="" alt="" />
    </div>
  </div>
);

Modal.propTypes = {};

export default Modal;
